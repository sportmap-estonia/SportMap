$kind: http-request
name: Get All Posts
method: GET
url: '{{baseUrl}}/api/feed'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
description: Returns a list of all verified posts.
