$kind: http-request
name: Get All Places
method: GET
url: '{{baseUrl}}/api/places'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
description: Returns a list of all approved places.
