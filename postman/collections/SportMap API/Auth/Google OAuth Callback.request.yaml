$kind: http-request
name: Google OAuth Callback
method: GET
url: '{{baseUrl}}/api/auth/callback/google'
order: 2000
auth:
  type: noauth
description: 'OAuth2 callback endpoint. Called by Google after authentication. Sets an HttpOnly access_token cookie and redirects to the frontend.'
